#can group digits using underscores
universe_age = 14_000_000_000
print(universe_age)

#multiple assignments
x, y, z = 0,0,0

#constants is a variable that stays the same throughout the 
#life of a program, use caps so python knows it should never be changed
MAX_CONNECTIONS = 5000
